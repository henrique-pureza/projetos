import { StyleSheet } from "react-native";

export const CSS = StyleSheet.create({
    scrollView: {
        height: "100%",
        paddingHorizontal: 20
    },
    actionButton: {
        width: "50%"
    },
    divider: {
        marginTop: 20
    },
    addButton: {
        position: "absolute",
        bottom: 25,
        right: 25
    }
});
